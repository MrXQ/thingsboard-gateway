# ThingsBoard IoT Gateway - 部署指南

## 环境要求

- Python >= 3.10
- Git

## 1. 克隆仓库

```bash
git clone -b release/3.8.2-sae-main https://github.com/MrXQ/thingsboard-gateway.git
cd thingsboard-gateway
```

## 2. 安装依赖

```bash
pip install -r requirements.txt
```

如需使用特定协议的连接器（Modbus、OPC-UA、BLE、BACnet 等），请安装完整依赖：

```bash
pip install -r requirements-full.txt
```

## 3. 安装项目包（必需）

网关使用绝对导入，因此运行前必须安装项目包。使用**可编辑模式**安装，使本地代码修改能立即生效：

```bash
pip install -e .
```

## 4. 配置网关

编辑 `thingsboard_gateway/config/tb_gateway.json`，更新 ThingsBoard 连接配置：

```json
{
  "thingsboard": {
    "host": "YOUR_THINGSBOARD_HOST",
    "port": 1883,
    "security": {
      "type": "accessToken",
      "accessToken": "YOUR_ACCESS_TOKEN"
    }
  }
}
```

| 字段 | 说明 |
|------|------|
| `host` | ThingsBoard 服务器地址（如 `thingsboard.cloud` 或 `127.0.0.1`） |
| `port` | MQTT 端口（默认 `1883`） |
| `accessToken` | 在 ThingsBoard 平台中获取的设备访问令牌 |

根据需要在 `"connectors"` 数组中添加连接器。示例连接器配置位于 `thingsboard_gateway/config/` 目录下（如 `mqtt.json`、`modbus.json`、`opcua.json`）。

## 5. 运行网关

```bash
python thingsboard_gateway/tb_gateway.py
```

或使用安装后的命令行脚本：

```bash
thingsboard-gateway
```

### 其他运行方式

| 方式 | 命令 |
|------|------|
| 热重载（文件变更时自动重启） | `python thingsboard_gateway/tb_gateway.py true` |
| 自定义配置目录 | `TB_GW_CONFIG_DIR=/path/to/config python thingsboard_gateway/tb_gateway.py` |
| 调试模式（在端口 5678 启动 debugpy） | `TB_GW_DEV_MODE=true python thingsboard_gateway/tb_gateway.py` |
| 交互式终端 | `tb-gateway-shell` |

## 6. 构建 Windows 可执行文件（.exe）

除了上述 Python 脚本运行方式外，还可以将网关打包为独立的 Windows `.exe` 文件，无需安装 Python 环境即可在 Windows 上运行。

### 6.1 安装额外依赖

```bash
pip install -r requirements-sae.txt
```

> **注意：** `requirements-sae.txt` 与 `requirements.txt` 分开管理，以保持与 ThingsBoard 官方上游仓库的兼容性。

### 6.2 构建

```bash
python tb_gateway_windows/build/build.py
```

构建完成后，输出目录结构如下：

```
dist/
  ├── tb-gateway.exe    # 独立可执行文件（约 40MB）
  ├── config/           # 用户可编辑的配置文件（从 thingsboard_gateway/config/ 复制）
  ├── extensions/       # 自定义扩展目录（空）
  └── logs/             # 运行时日志目录
```

### 6.3 配置

编辑 `dist/config/tb_gateway.json`，设置 ThingsBoard 连接信息（参见上方第 4 节）。

**重要：** 确保 `managerEnabled` 设为 `false` 或删除该字段（exe 模式下不支持多进程管理器）。

### 6.4 运行

```bash
dist\tb-gateway.exe
```

支持自定义配置目录：

```bash
dist\tb-gateway.exe --config-dir C:\path\to\config
```

运行后系统托盘会显示网关图标：

| 图标颜色 | 状态 |
|---------|------|
| 黄色 | 启动中 |
| 绿色 | 已连接 ThingsBoard |
| 红色 | 连接断开 |
| 橙色 | 部分连接器离线 |

右键点击托盘图标可查看状态详情、打开配置/日志目录、重启网关或退出。
