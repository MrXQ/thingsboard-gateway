# SAE 定制功能额外依赖
# ============================================================
# 此文件包含所有 SAE 定制功能的额外依赖（如 Windows exe 构建等）。
# 与 requirements.txt 分开管理，以保持与 ThingsBoard 官方上游仓库的兼容性，
# 方便执行 git pull 获取官方更新，而不产生合并冲突。
#
# 安装方式：
#   pip install -r requirements.txt       # 先安装网关核心依赖
#   pip install -r requirements-sae.txt   # 再安装 SAE 定制功能依赖
# ============================================================

# --- Windows Native Exe ---

pystray>=0.19
Pillow>=10.0
pyinstaller>=6.0

# --- Collect (PLC Data Collection) ---

JPype1>=1.4.0

# --- Collect (Log Collector) ---

watchfiles>=1.0
chardet>=5.0
